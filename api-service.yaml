apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 80         # Porta externa (pode continuar 80)
      targetPort: 8080 # Ajustado para 8080, que é onde o container está ouvindo
  type: NodePort
